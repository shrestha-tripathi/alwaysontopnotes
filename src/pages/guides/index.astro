---
import Layout from "../../layouts/Layout.astro";
import Nav from "../../components/Nav.astro";
import Footer from "../../components/Footer.astro";
import { site, absoluteUrl } from "../../site.config";
import { pseo } from "../../data/pseo";

const title = "Floating Sticky Notes Guides";
const description = `Step-by-step guides for keeping notes on top on Windows, Mac and Chromebook, and over Zoom, Teams, Meet, YouTube and games with ${site.name}. Free, notes stay on your device.`;
const itemList = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  itemListElement: pseo.map((e, i) => ({
    "@type": "ListItem",
    position: i + 1,
    url: absoluteUrl(`/guides/${e.slug}`),
    name: e.h1,
  })),
};
const breadcrumbSchema = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    { "@type": "ListItem", position: 1, name: "Home", item: absoluteUrl("/") },
    { "@type": "ListItem", position: 2, name: "Guides", item: absoluteUrl("/guides") },
  ],
};
---

<Layout title={title} description={description} customBreadcrumb jsonLd={[itemList, breadcrumbSchema]}>
  <Nav />
  <main class="mx-auto max-w-3xl px-4 py-12 sm:px-6 sm:py-16">
    <nav class="text-sm text-[var(--color-fg-muted)]" aria-label="Breadcrumb">
      <a href="/" class="hover:text-[var(--color-fg)]">Home</a>
      <span class="px-1.5">/</span>
      <span class="text-[var(--color-fg-muted)]">Guides</span>
    </nav>
    <header class="mt-6">
      <h1 class="text-4xl font-bold tracking-tight sm:text-5xl">Guides</h1>
      <p class="mt-4 text-lg text-[var(--color-fg-muted)]">
        Practical, honest how-tos for keeping a note visible above other apps — what works, what doesn't, and the
        browser requirements. Every guide uses the same free notes app that runs entirely in your browser.
      </p>
    </header>
    <ul class="mt-10 grid gap-4 sm:grid-cols-2">
      {pseo.map((e) => (
        <li class="rounded-lg border border-[var(--color-border)] bg-[var(--color-bg-subtle)] p-5">
          <a href={`/guides/${e.slug}`} class="font-semibold text-[var(--color-fg)] hover:text-[var(--color-accent-strong)]">{e.h1}</a>
          <p class="mt-2 text-sm text-[var(--color-fg-muted)]">{e.metaDescription}</p>
        </li>
      ))}
    </ul>
  </main>
  <Footer />
</Layout>
